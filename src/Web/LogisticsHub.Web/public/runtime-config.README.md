runtime-config.json is deployment-specific and can be replaced without rebuilding the Angular app.

Serve this file with no-cache/no-store headers or a short TTL in Azure, CDN, or static hosting so config changes are picked up promptly.
